# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

*~
/dist/
.DS_Store
# .eztoolkit-bin is a TRACKED marker file (not ignored) — used by
# scripts/release publish to verify the target dir is the real repo.
.claude/settings.local.json
